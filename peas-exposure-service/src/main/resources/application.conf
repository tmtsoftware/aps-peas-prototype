include required("logging.conf")

auth-config {
  realm = TMT
  client-id = tmt-backend-app
}

http-server {
  port = 8086
  prefix = "APS.PeasExposureService"
}

exposure-service {
  # Root directory that exposureStoreCompleted's `filename` parameter is
  # resolved against.
  # TODO(Scott): confirm against the real DMS exposure-store ICD -- this is
  # currently just a placeholder local path.
  fits-root-dir = ${?PSH_EXPOSURE_FITS_ROOT_DIR}
}
